import React, { Component } from 'react';
import { Container, Header, Content,Button,Item, Input,Text,
  Body,Left,Title,Right,List,ListItem,Thumbnail,Textarea} from 'native-base';
import { Col, Row, Grid } from 'react-native-easy-grid';
import { StackNavigator, TabBarBottom, TabNavigator } from "react-navigation";
import {AppRegistry,Image,View,TouchableOpacity,FlatList,Modal,SectionList}from 'react-native';
let sco ={};
// import Dome from './Dome'; 
export default class A1 extends Component {
  constructor(props) {
    super(props); 
    main.init('A1',this); 
    sco = this;
  }
    // fn(){
    //   alert(this.props.inde)
    // }
  render() {
    return (
        <View style={{flexDirection:'row'}}>

        {/* <SectionList
          renderItem={({item}) => <ListItem title={item.title} />}
          renderSectionHeader={({section}) => <H1 title={section.key} />}
          sections={[ // 不同section渲染相同类型的子组件
            {data: [conf.Teditedlist], key: ...},
            {data: [...], key: ...},
            {data: [...], key: ...},
          ]} */}
        />
        {/* <SectionList
          renderItem={({item}) => <ListItem >{item.text}</ListItem>}
          renderSectionHeader={({section}) => <Text >{section.key}</Text>}
          sections={[
            {data: [conf.Teditedlist],},
           
          ]}
        /> */}
           {/* <SectionList
           sections={[conf.Teditedlist]}
            renderItem={({item}) => (
                <Text style={{width:80,height:40,backgroundColor:'#f60',marginTop:3,}}>{item.text}</Text> 
              )}
          /> */}
          {/* <FlatList
            style={{width:140,height:200,backgroundColor:'#000'}}
            data={sco.gh}
           
          />  */}
        </View>  
    )

}}